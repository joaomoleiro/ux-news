source "https://rubygems.org"

# Usa o mesmo conjunto de gems que o GitHub Pages corre em produção,
# para o build local bater certo com o deploy.
gem "github-pages", group: :jekyll_plugins

# Necessário em Ruby 3.0+ (deixou de vir incluído por defeito)
gem "webrick", "~> 1.8"
